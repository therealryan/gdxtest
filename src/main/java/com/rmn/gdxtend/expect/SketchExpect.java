package com.rmn.gdxtend.expect;

/**
 * Supports tests where the result is a drawing
 */
public class SketchExpect extends AbstractSketchExpect<SketchExpect> {
	// all behaviour in the superclass, this is just to avoid generics syntax with
	// every use
}
